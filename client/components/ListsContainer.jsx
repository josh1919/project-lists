import React from 'react';

export default class ListsContainer extends React.Component {
  render() {
    return (
     <div>
       <h1>Lists container</h1>
       <h2>Sort out where each list will go here.</h2>
       <h3>there will be potentially four lists. New(received but not started), in progress, outstanding (work complete but money not collected), complete </h3>
     </div>);
  }
}
