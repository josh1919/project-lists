import React from 'react';

export default class ListItem extends React.Component {
  render() {
    return (
     <div>
       <h4>prop:name of list</h4>
       date complete,
       amount due/estimate (depending on where it is in progress)

      </div>);
  }
}
